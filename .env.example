# Neon PostgreSQL — pooled connection (use for app runtime)
# Get from: Neon Console → Connection Details → "Pooled connection"
DATABASE_URL="postgresql://neondb_owner:npg_vK8ayuQVC1WP@ep-nameless-flower-a4vv1pdq-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Neon PostgreSQL — direct connection (use for Prisma migrations)
# Get from: Neon Console → Connection Details → "Direct connection"
DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Optional: protect cron endpoint for scheduled publishing
CRON_SECRET="generate-a-random-secret"

# Cloudinary (unsigned upload) — get from your Cloudinary dashboard
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_UPLOAD_PRESET="your-upload-preset"
